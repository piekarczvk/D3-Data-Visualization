/**
 * Scatter plot class
 */
export default class ScatterPlot{ 

    // Use the code from BarChart.js as a template to complete this class

    // dots can be rendered with circle (instead of rect)

    // a circle has 3 important attribute: cx (center x), cy (center y) and r (radius)
    // you can use fixed values for radiuses
}